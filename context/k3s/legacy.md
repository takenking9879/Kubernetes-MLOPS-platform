# k3s/ — Legacy Files (Safe to Delete)

| File | Reason |
|------|--------|
| `k3s/airflow/dags/dag.py` | Empty stub; no DAG definitions; no functionality |
| `k3s/sky/ray-gpu-training.yaml` | Generic version superseded by `ray-gpu-training-runpod.yaml` + `ray-gpu-training-vast.yaml` (provider-specific) |
| `k3s/sky/ray-llm-training.yaml` | Generic version superseded by `ray-llm-training-runpod.yaml`, `ray-llm-training-vast.yaml`, `ray-llm-training-aws.yaml` |
| `k3s/sky/vllm-serving.yaml` | Generic version superseded by `vllm-serving-runpod.yaml` + `vllm-serving-vast.yaml` |
| `k3s/sky/hello-sky.yaml` | SkyPilot "Hello World" test; not used in production |
| `k3s/sky/vast-test.yaml` | Vast.ai test/debug YAML; not used in production |
| `k3s/params.yaml` | Developer reference only; not used at runtime since 2026-02-27. Actual params now generated by backend and uploaded to S3 under `runs/{type}/{run_id}/params_*.yaml` |
| `k3s/kuberay/kuberay-job-gpu.yaml` | GPU-specific RayJob variant superseded by `kuberay-job.yaml` with dynamic patching |

## What NOT to delete (looks legacy but is ACTIVE)

| File | Why keep |
|------|---------|
| `k3s/airflow/dags/training_pipeline.py` (`training_pipeline` DAG) | ACTIVE: used for local KubeRay training path (non-SkyPilot); `training_pipeline_skypilot` is cloud path |
| `k3s/airflow/dags/ml_pipeline_dag.py` | ACTIVE: UI-triggered flexible mode pipeline |
| `k3s/params.yaml` | Keep if needed as developer reference; just don't use at runtime |
| `k3s/spark/preprocess/dsl_001.yaml` | ACTIVE: referenced as default DSL; overridden per run via S3 but used as baseline |
