# =========================
# Runtime deps for tabular serving on provider-native GPU templates
# (RunPod / Vast.ai — torch is PRE-INSTALLED in provider base image, DO NOT add torch here)
# Used by: tabular-serving-single.yaml, tabular-serving-multinode.yaml
#
# Subset of requirements_tabular_runtime.txt:
#   - ray[serve] only (no data/train/tune — faster setup, not needed for inference)
#   - No kubernetes (unused)
#   - No deepspeed (training-only, never needed for serving)
#   - Adds starlette + uvicorn (Ray Serve HTTP layer)
# =========================

packaging==25.0
ray[serve]==2.53.0

# =========================
# Core ML / Data
# =========================
numpy==2.2.6; python_version < "3.11"
numpy==2.3.3; python_version >= "3.11"
pandas==2.3.3
pyarrow==22.0.0
scikit-learn==1.8.0

# =========================
# Gradient Boosting
# =========================
xgboost==3.1.3

# =========================
# Experiment Tracking
# =========================
mlflow==3.7.0

# =========================
# Cloud / Config / HTTP
# =========================
boto3==1.42.28
python-dotenv==1.2.1
PyYAML==6.0.3
prometheus-client==0.24.1
starlette
uvicorn
